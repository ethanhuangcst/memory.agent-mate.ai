# 复制为 .env 并填入真实值；.env 不进 Git（已在 .gitignore）
# 服务器侧存放：/opt/ai-memory-mcp/.env
# 本地对应文件：hk_vps_4/deploy/.env.local（gitignored）

# ghcr.io 上真实存在的 tag；禁用 latest（Portainer 不一定重新 pull）
# 版本坐标的唯一真相源：hk_vps_4/upstream.lock（当前 v0.10.0 / schema 80 / 指纹已校验）
#   查看：make pin          校验是否有更新版：make preflight
IMAGE_TAG=0.10.0

# DashScope（qwen）API key — 供 [llm] 与 [embeddings] 使用
DASHSCOPE_API_KEY=
