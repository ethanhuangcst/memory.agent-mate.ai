# link-check.sh 的允许清单：把「已知且暂不修」的悬空链接**显式化**，而非静默放宽。
#
# 格式（每行一条）：
#   <源文件相对路径（相对仓库根）> <链接目标>   # 理由（必须写）
# 目标写 `*` 表示：该文件的全部悬空链接暂允（用于「用户指定零改动」或「待移除」的文件）。
#
# ⚠️ 本清单是债务台账，不是垃圾桶 —— 每新增一行都要写清理由与预期清理时机；
#    清理后请删除对应行（`make doc-links` 会持续提醒，直到债务清空）。

# ── 模板文件：用户明确「将来移除；本次不改、不被引用」，内部链接随原样保留 ──────
specs/hk_vps_4_settings.md                 *   # 模板文件，待移除；本次不改不引用（关键信息已摘编进 architecture/deployment）
specs/vps4_new_deployment_instruction.md   *   # 同上
