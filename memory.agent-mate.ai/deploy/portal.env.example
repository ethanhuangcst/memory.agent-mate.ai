# 门户 stack 环境文件 —— 模板（Sprint 4 落地时复制为 portal.env 并填值）
#
# 存放位置：
#   服务器：/opt/ai-memory/portal.env（chmod 600）
#   本机开发：memory.agent-mate.ai/deploy/portal.env（已在 .gitignore，永不入仓）
#
# 为什么与主 stack 的 .env 分开：
#   门户是公网可达组件；独立 key ⇒ 出事时只需吊销/轮换门户这一把，主线服务不受牵连，
#   用量与异常也能按 key 区分归因。
#
# ⚠️ 填值要求（否则检索会静默降级，工具却照常返回成功）：
#   1) 必须是「门户专用」key，且与主 .env 的 DASHSCOPE_API_KEY **同工作空间 / 同模型权限**
#      —— 同工作空间建 key ⇒ 模型与向量维度（1024）天然一致；跨工作空间会导致嵌入不可用
#   2) 变量名必须与主 stack **一致**（`config.toml` 的 `api_key_env` 只写变量名 ⇒ 门户不改任何配置）
#   3) 本文件**只放这一把 key**：门户容器不挂 docker socket、不放 root 凭据

# 门户 stack 的**密钥**文件样例（服务器 `/opt/ai-memory/portal.env`，落地时 `chmod 600`）。
#
# **只放密钥**：非密钥配置（21 个 `PORTAL_*` 键：面隔离 / 存储 / 会话 / 身份 / i18n / 日志）
# 一律写在 `portal.compose.yml` 的 `environment:` —— 那是它们的**真源**（步骤见
# `../specs/deployment.md` §12.5；一致性由 `make deploy-doc-audit` 把住）。
DASHSCOPE_API_KEY=
