ai-memory-mcp/
tmp-memory.md
.DS_Store
.env
# 本机 secret 文件（真实 IP / SSH 密码 / 管理台账号，永不入库）
secrets.local*
*.key

# 本地部署：真实 API key 与真实端点（由模板填充而来，永不入库）
# ⚠️ 用路径无关模式：目录若再改名，含密文件不会因规则失配而被误提交（本仓为公开仓）
**/deploy/.env.local
**/deploy/config.local.toml
# 本地 compose 运行时派生文件（由上面两个文件复制而来，compose 固定引用 .env / config.toml）
**/deploy/.env
**/deploy/config.toml
# 门户 stack 的环境文件（含门户专用 MaaS key，永不入库；模板见 deploy/portal.env.example）
**/deploy/portal.env

# IDE / 工具数据：Plan Mode 计划文件等（公开仓不入库，防 `git add .` 误带）
.codebuddy/

# 门户（PSP-W1 起）：依赖与本机运行产物不入库
#   注：环境文件 admin_portal/.env 已被上方 `.env` 规则覆盖，此处不重复；
#       `.env.local` 是**不同 basename**，`.env` 规则不覆盖它 —— 必须显式忽略
#       （快速路径环境文件；用路径无关模式，目录改名也不会失配）
**/admin_portal/.env.local
memory.agent-mate.ai/admin_portal/node_modules/
memory.agent-mate.ai/admin_portal/.portal-data/
memory.agent-mate.ai/admin_portal/coverage/
memory.agent-mate.ai/admin_portal/tests/e2e/artifacts/
